= content_for :section_title do
  Administração

= content_for :page_title do
  Editar sala

.box.box-primary{style: 'width: 50%'}
  .box-header
    %h3.box-title= @room.name

  = simple_form_for [:admin, @room] do |f|
    = render 'form_fields', f: f